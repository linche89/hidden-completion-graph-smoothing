                                                                        Automatica 95 (2018) 63–72



                                                              Contents lists available at ScienceDirect


                                                                          Automatica
                                                    journal homepage: www.elsevier.com/locate/automatica




A distributed Kalman filtering algorithm with fast finite-time
convergence for sensor networks✩
Zongze Wu a , Minyue Fu b,a, *, Yong Xu a , Renquan Lu a
a
    School of Automation, Guangdong University of Technology, and Guangdong Key Laboratory of IoT Information Technology, Guangzhou 510006, China
b
    School of Electrical Engineering and Computer Science, The University of Newcastle, NSW 2308, Australia



article                info                            a b s t r a c t
Article history:                                       This paper proposes a new distributed algorithm for Kalman filtering. It is assumed that a linear discrete-
Received 24 January 2017                               time dynamic system is monitored by a network of sensors with some being active and some idle. The goal
Received in revised form 9 December 2017               of distributed state estimation is to devise a distributed algorithm such that each node can independently
Accepted 17 April 2018
                                                       compute the optimal state estimate by using its local measurements and information exchange with
Available online 29 May 2018
                                                       its neighbours. The proposed algorithm applies to acyclic network graphs (i.e., tree graphs) with fast
                                                       finite-time convergence, but is also applicable to cyclic graphs by combining it with a distributed loop
Keywords:
Distributed estimation                                 removal algorithm. The proposed algorithm enjoys low complexities, robustness against transmission
Distributed Kalman filtering                           adversaries and asynchronous implementability. The proposed distributed algorithm also applies to
Sensor networks                                        maximum likelihood estimation and weighted least-squares estimation, as special cases. With simple
Maximum likelihood estimation                          modifications, the proposed algorithm also applies to an important problem in signal processing called
Weighted least-squares estimation                      distributed field estimation.
Distributed field estimation                                                                                               © 2018 Elsevier Ltd. All rights reserved.




1. Introduction                                                                          gossip protocol was given in Li, Kar, Moura, Poor, and Cui (2015);
                                                                                         DKF using diffusion strategies was studied in Cattivelli and Sayed
   Distributed Kalman filtering (DKF) has been an extremely active                       (2010) and Hu, Xie, and Zhang (2012); DKF with out-of-sequence
research topic for over a decade, in response to rapid development                       measurements was treated in Shen, Song, Zhu, and Luo (2009), and
and vast deployment of low-cost sensors and sensor networks.                             fusion-centre based DKF designs were shown in Song, Xu, and Zhu
The technical challenge is how to migrate the well-established                           (2014) and Xu, Song, Luo, and Zhu (2012). See Mahmoud and Khalid
central (or traditional) Kalman filtering (KF) approach (Anderson                        (2013) for a recent survey on DKF. Related works also include dis-
& Moore, 1979; Kalman, 1960) to complex large-scale dynamic                              tributed maximum likelihood estimation (Zhao & Nehorai, 2007)
systems with measurements distributed over a large geographical                          and distributed weighted least-squares estimation (Marelli & Fu,
area (Khan & Moura, 2008). Available DKF algorithms are already                          2015).
abundant. For example, a one-step prediction algorithm was in-                               Numerous applications of DKF have been reported in the lit-
troduced in Zhou (2013); a distributed iterate-collapse inversion                        erature, ranging from environmental monitoring to surveillance,
algorithm in conjunction with a bipartite fusion graph was in-                           detection, tracking and object classification. Target tracking using a
troduced in Khan and Moura (2008) for spatially sparse systems;                          sensor network over a large geographical area is an active research
distributed fusion estimation was proposed in Chen, Zhang, and Yu                        topic recently, and DKF has been shown to play an important role in
(2014a, b) and Chen, Zhang, Yu, Hu, and Song (2015); DKF using                           this application (Medeiros, Park, & Kak, 2008; Zhou, Fang, & Hong,
quantized information were studied in Li, Kar, Alsaadi, Dobaie,                          2013). In fact, this line of research can be traced back to Durrant-
and Cui (2015), Riberio, Giannakis, and Roumeliotis (2006) and                           Whyte and Rao (1991) in 1991 and Regazzoni (1994) in 1994. DKF
Song, Yu, and Zhang (2014); a DKF design using the well-known                            also finds wide applications in plant-wide control systems (Vadi-
                                                                                         gepalli & Doyle, 2003), stochastic nonlinear systems with commu-
  ✩ The material in this paper was not presented at any conference. This paper was       nication delays and packet losses (Wang, Fang, & Liu, 2015), clock
recommended for publication in revised form by Associate Editor Bert Tanner under        synchronization for sensor networks (Luo & Wu, 2013), wireless
the direction of Editor Christos G. Cassandras.                                          sensor networks (Riberio & Giannakis, 2006), spatial estimation
    *
    Corresponding author at: School of Electrical Engineering and Computer Sci-
ence, The University of Newcastle, NSW 2308, Australia.
                                                                                         (Cortés, 2009), power networks (Kanna, Dini, Xia, Hui, & Mandic,
    E-mail addresses: zzwu@scut.edu.cn (Z. Wu), minyue.fu@newcastle.edu.au               2015; Roshany-Yamchi et al., 2013; Sun, Fu, Wang, & Zhang, 2015;
(M. Fu), yxu@hdu.edu.cn (Y. Xu), rqlu@gdut.edu.cn (R. Lu).                               Tai, Lin, Fu, & Sun, 2013). We will also show in this paper a novel

https://doi.org/10.1016/j.automatica.2018.05.012
0005-1098/© 2018 Elsevier Ltd. All rights reserved.
64                                                     Z. Wu et al. / Automatica 95 (2018) 63–72


application of DKF in distributed field estimation where a sensor             and storage complexities, robustness against transmission loss and
network is used to estimate the parameters of a physical field                delay and asynchronous implementability. The algorithm can run
over a large geographical area, a problem with vast applications              in either a point-to-point communication mode between neigh-
on its own (see, e.g., Martinez, 2010; Talarico, Schmid, Alkhweldi,           bouring sensors (for better privacy) or in a broadcast mode (for
& Valenti, 2014; Wang, Ishwar, & Saligrama, 2008).                            lower communication burden).
     One common approach to DKF is to use an average consensus                    We emphasize that the above target tracking setting is used to
strategy as introduced in Xiao and Boyd (2004); see Carli, Chiuso,            motivate the proposed DKF algorithm. Other applications of the
Schenato, and Zampieri (2008), Das and Moura (2015), Kar and                  proposed DKF algorithm include cascading failure monitoring in a
Moura (2011), Song, Yu et al. (2014), Xu et al. (2012), and Zhou              power network, vehicle tracking in a transportation network, fire
et al. (2013) for examples of this approach. The main shortcomings            monitoring in a forest, etc. In particular, we will study the appli-
of this approach include (1) usually only asymptotic convergence              cation to distributed field estimation in detail. To help illustrate
is guaranteed, meaning that an infinite number of iterations is               the proposed DKF, two simulation examples will be given, one on
required in theory; (2) stopping criteria are difficult to give for           single and multiple target tracking and one on distributed field
practical applications; (3) only sub-optimal estimates are usually            estimation.
given. Another common problem with many existing DKF algo-                        The rest of the paper is organized as follows: Section 2 formu-
rithms is that a fusion centre is required, which means that they             lates the DKF problem; Section 3 gives the proposed DKF algo-
are not fully distributed. Approaches of this kind include Chen et al.        rithm; Section 4 details a number of properties of this algorithm;
(2014a, b, 2015), Shen et al. (2009), Song, Xu et al. (2014) and Xu et        Section 5 discusses its application to distributed field estimation;
al. (2012). We also note that fusion centre is also commonly used in          Section 6 demonstrates the algorithm via some simulation exam-
the so-called parallel Kalman filtering where local measurements              ples; and Section 7 concludes the paper.
are used to produce local estimates that are then fused together in
a fusion centre; see, e.g., Hashemipour, Sumit, and Laub (1988). The          2. Problem formulation
need for fully distributed DKF with good optimality properties and
low computational, communicational and storage complexities is                    Consider a dynamic model for a target system described by
urgent. These features are essential to make DKF scalable to large-
scale sensor networks.                                                        x(t + 1) = A(t)x(t) + w (t),                                             (1)
     In this paper, we consider a sensor network used to detect,
monitor and track targets within the geographical area covered by             where t = 0, 1, . . . is the time index, x(t) ∈ R is the state,
                                                                                                                                          n

the network. Each target is modelled as a linear dynamic system,              w(t) ∈ Rn is a zero-mean i.i.d. Gaussian noise with covariance
suitable for describing the motion of a moving target or changes              W (t) ≥ 0, and A(t) ∈ Rn×n is the (possibly) time-varying transition
in time-varying parameters. The goal of this paper is to devise a             matrix. The initial state x(0) is an independent Gaussian variable
distributed algorithm that allows us to estimate the state of each            with mean x0 and covariance P0 > 0.
target system. The algorithm needs to have low complexities per                  The target system is measured by a network of sensors which
node (in terms of communication, computation and storage) so                  can be represented by a graph G (t) = {V (t), E (t)} with a set of
that it is scalable to large-scale sensor networks. For convenience,          nodes V (t) and a set of edges E (t) ⊂ {(i, j) : i ̸ = j, i, j ∈ V (t)}. The
we mainly consider the tracking of a single target system, as the             set V (t) = I (t) ∪ S (t), where S (t) is a set of sensing nodes with
tracking of multiple targets can be achieved by a multiple number             measurements and I (t) is an idle set consisting of nodes without
of single target trackers. We consider the scenario where the target          measurements. Each node i ∈ S (t) has a measurement:
system is measured by a small subset of active sensors at each
time instant, while the rest of the sensors are idle. The set of              yi (t) = Ci (t)x(t) + vi (t),                                            (2)
active sensors is allowed to vary for each time. The use of idle              where yi (t) ∈ Rri , Ci (t) ∈ Rri ×n is the (possibly) time-varying
nodes is motivated by applications where only a small fraction of             measurement vector, vi (t) is the measurement noise which is a
sensors are able to measure information for a particular system. For          zero-mean i.i.d. Gaussian noise with covariance Ri (t) > 0. Stacking
example, a surveillance network may be responsible to monitor                 up all the measurements, we get
certain types of targets over a large geographical area. A moving
target may be observed only by a small number of sensors around               y(t) = C (t)x(t) + v (t).                                                (3)
it, but the information about it may need to be distributed among
the whole network so that the target can be tracked as it traverses           The covariance of v (t) is R(t) = diag{Ri (t)}. It is assumed through-
within the network.                                                           out the paper that the system with (1) and (3) is observable.
     Following the standard (central) KF approach, we also divide                  We assume that G (t) is undirected and acyclic. A graph is called
the DKF problem into two steps, a maximum likelihood estimation               undirected if each edge is undirected. A graph is called acyclic if
(MLE) step and a one-step forward prediction step. The core tech-             it is connected and has no loops, i.e., it is a tree graph. We will
nical issue is how to carry out the MLE step in a fully distributed           show how to deal with cyclic graphs later. Denote by Ni (t) the set
manner. For this, we propose a fully distributed maximum likeli-              of neighbouring nodes connected to node i, and denote by |Ni (t)|
hood estimation (DMLE) algorithm. Under the assumption that the               the cardinality of Ni (t). We assume that |Ni (t)| ≪ |V (t)| for each
communication graph for the sensor network is acyclic (i.e. it is a           i ∈ V (t). We denote by d(t) the diameter of the graph G (t), which
tree graph), the algorithm delivers the same (optimal) estimate as            is the length of the longest path between two nodes.
given by a central MLE algorithm, but with the advantage of fast                   For notational convenience, we will suppress the time depen-
convergence. That is, the proposed DMLE algorithm converges in                dence of the system parameters (A(t), C (t), W (t), R(t)) as well as
a finite number of iterations (this number equals the maximum                 those of the network graph (G (t), V (t), I (t), S (t), E (t), Ni (t), d(t)).
diameter of the graph). For sensor networks with a cyclic graph, we           In addition, with some abuse of notation, i ∈ G means ∈ V , and
can apply a distributed depth-first-search (DFS) algorithm to con-            (i, j) ∈ G means (i, j) ∈ E .
vert the given graph to a spanning tree before applying the DMLE                   It is well known (Anderson & Moore, 1979) that the optimal
algorithm. We will show that the proposed DKF algorithm, which                state estimation, in the maximum likelihood sense, is given by the
is based on the aforementioned DMLE algorithm, enjoys a number                celebrated Kalman filter (KF), which we will call central Kalman
of nice properties, including low computational, communicational              filter. Given the system model (1) and the measurement model
                                                          Z. Wu et al. / Automatica 95 (2018) 63–72                                                        65


(3), the optimal state estimate x̂(t + 1) of x(t + 1), conditioned on            Algorithm 1 (Distributed Maximum Likelihood Estimation)
measurements y(0), y(1), . . . , y(t), is given by                               Initialization: For each node i and each j ∈ Ni ,

                                                                                                                       i Ci and αi→j (0) = Ci Ri yi (t),
                                                                                                                        1                    T −1
x̂(t + 1) = Ax̂(t) + AGt (y(t) − C x̂(t))                              (4)            • if i ∈ S , set Qi→j (0) = CiT R−
with                                                                                  • else (i ∈ I ), set Qi→j (0) = 0 and αi→j (0) = 0;
           T           T
Gt = Pt C (t)(CPt C + R)        −1
                                     ,                                 (5)            • then transmit the above to node j.
where Pt is the estimation error covariance for x̂(t) given by
                                                                                 Main loop: At iteration k = 1, 2, · · · , d, for each node i,
Pt +1 = APt AT + W
                                                                                      • if i ∈ S , compute
       − APt C T (CPt C T + R)−1 CPt AT .                              (6)
                                                                                                                       ∑
                                                                                                        1
   An alternative expression for the above is as follows (which we                       Qi (k) = CiT R−
                                                                                                       i Ci +                 Qj→i (k − 1);           (12)
will use for developing a distributed estimation algorithm):                                                           j∈Ni
                                                                                                                          ∑
    • Update:                                                                            αi (k) = CiT R−1
                                                                                                       i yi (t) +                αj→i (k − 1),        (13)
                                                                                                                          j∈Ni
         x̃(t) = x̂(t) + Gt (y(t) − C x̂(t));                          (7)
                           T
           P̃t = Pt − Pt C (CPt C + R)   T   −1
                                                  CPt .                (8)            • else (i ∈ I ), comptue
                                                                                                 ∑
    • Prediction:                                                                       Qi (k) =       Qj→i (k − 1);                                  (14)
                                                                                                      j∈Ni
         x̂(t + 1) = Ax̃(t);                                           (9)                            ∑
               Pt +1 = AP̃t A + W .
                           T
                                                                      (10)               αi (k) =            αj→i (k − 1),                            (15)
                                                                                                      j∈Ni
    The problem of distributed Kalman filtering (DKF) is to devise an
iterative algorithm which runs on every node i ∈ Vt to compute an                     • then for each j ∈ Ni ,
estimate of x(t) such that after a certain number of iterations, each
node’s estimate, denoted by x̂i (t), will converge to the optimal state                  Qi→j (k) = Qi (k) − Qj→i (k − 1);                            (16)
estimate x̂(t) given by the central KF.                                                  αi→j (k) = αi (k) − αj→i (k − 1),                            (17)
    Before ending this section, we need to clarify what we mean
                                                                                         and transmit them to node j.
by ‘‘distributed’’. In this paper, an algorithm is called distributed
if the following linear complexity constraints on communication,                 Termination:
computation and storage are satisfied:
                                                                                   P̃t = (Pt−1 + Qi (d))−1 ;                                          (18)
  (1) Local information exchange: Each node i can exchange in-
      formation with each j ∈ Ni only once per iteration.                        x̃(t) = P̃t (Pt x̂(t) + αi (d)).
                                                                                               −1
                                                                                                                                                      (19)
  (2) Local computation: Each node i’s computational load should
      be at most O(|Ni |) per iteration.
  (3) Local storage: Each node i’s storage should be at most O(|Ni |)
      over all iterations.                                                       Proof. In the sequel, for a given state estimate x̂ and the associated
                                                                                 estimation error covariance P > 0, we will denote the information
                                                                                 matrix as Q = P −1 and the scaled state estimate as α = Q x̂.
3. Distributed algorithm for Kalman filtering                                       Using the well-known matrix inverse lemma, (8) can be rewrit-
                                                                                 ten as
   In this section, we introduce our DKF algorithm, provide its key
property on acyclic graphs and analyse its complexities.                         P̃t−1 = Pt−1 + C T R−1 C .
                                                                                 In view of the composition of C and the fact that R is a block
3.1. Distributed maximum likelihood estimation
                                                                                 diagonal matrix, the above can be further rewritten as
    The main difficulty for distributed implementation of the KF lies                                 ∑
                                                                                                                 i Ci .
                                                                                                                   1
                                                                                 P̃t−1 = Pt−1 +             CiT R−                                    (20)
in the Update step (7)–(8). This step is also known as the maximum
                                                                                                      i∈S
likelihood estimation (Anderson & Moore, 1979). The problem is to
produce the posteriori estimate x̃(t) of x(t), given its prior estimate          It is also straightforward to verify that (7) can be rewritten as
x̂(t) and measurement y(t), i.e., we need to compute the following
                                                                                 x̃(t) = P̃t (Pt−1 x̂(t) + C T R−1 y(t))
conditional expectation (see Anderson & Moore, 1979):                                                         ∑
                                                                                                                          i yi (t)).
                                                                                                                           1
                                                                                       = P̃t (Pt−1 x̂(t) +           CiT R−                           (21)
x̃(t) = E {x(t)|x̂(t), y(t)}.                                         (11)                                     i∈S

Its solution is, of course, given by (7)–(8); see Anderson and Moore             A similar expression of the above can be traced back to
(1979).                                                                          Hashemipour et al. (1988).
    The proposed distributed maximum likelihood estimation al-                       Take any edge (i, j) ∈ E . Because G is a tree graph, G can be split
gorithm is given in Algorithm 1. It is assumed that the graph G is               into two sub-graphs Gi and Gj with a connecting edge (i, j) between
acyclic with diameter d.                                                         them. Here Gi is a tree graph with node i as the root node, and Gj is
                                                                                 a tree graph with node j as the root node, and they are obtained
                                                                                 from G by removing the edge (i, j). For each node in Gi , denote
Theorem 1. Under the assumption that G is acyclic with diameter d,               its layer by the number of hops it is away from node i. That is,
the estimate x̃(t) and the associated error covariance P̃t computed by           node i is called the layer-0 node; all the nodes one hop away from
Algorithm 1 coincide with those by (7)–(8).                                      node i are called layer-1 nodes, those two hops away are called
66                                                       Z. Wu et al. / Automatica 95 (2018) 63–72


layer-2 nodes, and so on. It is clear that the initialization steps set         3.2. Distributed prediction
Qi→j (0) as the information matrix and αi→j (0) as the scaled state
estimate for x(t) using the layer-0 information in Gi only. Then,                   The distributed implementation of the Prediction step (9)–(10)
tracing through (12)–(17) will lead us to see that after iteration              is easy, given that each node i ∈ V has a local estimate equal to
1, Qi→j (1) will be the information matrix for x(t) using layer 0               x̃(t) and the corresponding estimation error covariance equal to P̃t .
and layer 1 information in Gi only, and no information from Gj is               That is, each node i ∈ V computes (9)–(10) independently.
used. Similarly, αi→j (1) will be the scaled state estimate for x(t)
using layer 0 and layer 1 information in Gi only, without using                 3.3. Application to cyclic network graphs
information from Gj . This process goes on. So in iteration d, Qi→j (d)
and αi→j (d) will be the information matrix and the scaled state                    A main restriction of the proposed algorithm for DKF is that the
estimate for x(t), respectively, using all the information from layer           measurement network graph needs to be acyclic. For a cyclic graph,
0 to layer d in Gi , without using information from Gj . Similarly,             it is necessary to prune loop-forming edges so that the remaining
Qj→i (d − 1) and αj→i (d − 1) will be the information matrix and the            graph becomes loop free. The resulting graph is called a spanning
scaled state estimate for x(t), respectively, using all the information         tree, a tree graph with all the nodes in the original graph.
from layer 0 to layer d − 1 in Gj , without using information from Gi .
                                                                                    Constructing a spanning tree is a well-known classical problem
We combine them together (using (16)–(17)) to create
                                                                                in graph theory. The standard (and most popular) algorithms are
Qi (d) = Qi→j (d) + Qj→i (d − 1)                                                depth-first search (DFS) and breath-first search (BFS) (Goodrich &
αi (d) = αi→j (d) + αj→i (d − 1),                                               Tamassia, 2001; Skiena, 2008). Both algorithms can construct a
                                                                                spanning tree in O(|V | + |E |) time. However, these algorithms are
and note that node j is one hop away from node j and that the                   sequential, i.e., not distributed. Indeed, both algorithms start with
diameter of G is d. Then, it is clear that Qi (d) and αi (d) will be            an arbitrarily chosen root node. In DFS, one starts at the root and
the information matrix and the scaled state estimate for x(t), re-              explores as far as possible along each branch before backtracking
spectively, using all the nodes in G . In particular, these results are         and exploring another branch; whereas in BFS, one starts at the
independent of node i. Finally, (18) combines the prior information             root and explores the neighbouring nodes first, before moving to
matrixPt−1 with Qi (d) to create the posterior information matrix               the next level neighbours.
P̃t . Similarly, (19) combines the prior scaled state estimate Pt−1 x̂(t)           In our recent work (Xie & Fu, 2018), we proposed a distributed
with αi (d) to create the posterior scaled state estimate Pt−1 x̂(t) +          DFS algorithm (Algorithm 2) for constructing a spanning tree. The
αi (d) and then the posterior state estimate x̃(t).                             algorithm applies to a connected graph with diameter d, with the
                                                                                assumption that each node in the graph has a distinct numerical ID
Remark 2. We give some interpretations for Algorithm 1 . Follow-                number.
ing the proof of Theorem 1, we see that the Initialization part sets
the scaled state estimate αi→j (0) and the associated information
                                                                                Algorithm 2 (Distributed DFS Algorithm for Spanning Tree)
matrix Qi→j (0) for x(t) and allows them to be broadcast to all of
the neighbouring nodes of i. The steps (12)–(15) fuse the scaled                Initialization: Select a root node in G (by running max-consensus
estates and the information matrices together. For i ∈ I , only                 d iterations and setting the node with maximum ID number as the
the neighbouring information needs to be fused together; whereas                root node). Mark the root node as ‘‘visited" and and all other nodes
for i ∈ S , the measurement yi needs to be added as well. The                   as ‘‘unvisited’’. Transmit a token from the root node to each of its
steps (16)–(17) ensure that the information transmitted back to                 neighbouring nodes.
node j will not be ‘‘contaminated’’ by the information coming                   Iterations k = 1, 2, . . .: For each node i, do the following:
from node j previously. The avoidance of contamination marks
a crucial difference between the proposed distributed maximum                      (1) If it does not receive the token, do nothing;
likelihood estimation algorithm and many distributed algorithms
                                                                                   (2) If it is ‘‘unvisited" and it receives the token from only one
in the literature, and is the key for finite-time convergence.
                                                                                       neighbour, then mark the node as ‘‘visited", and relay the to-
Remark 3. We see from the proof of Theorem 1 that the computa-                         ken to all other neighbouring nodes without the ‘‘removal"
tion of P̃t−1 in (20) involves the fusion of the terms CiT R−1                         mark, except the incoming edge (i.e., the edge where the
                                                            i Ci for all
                                   T −1                                                token came from);
i ∈ S . Similarly, the fusion of Ci Ri yi (t) is needed for computing
x̃(t) in (21). Since the fusion is done iteratively, it is natural to              (3) If it is ‘‘unvisited" and it receives the token from multiple
ask what the minimum number of iterations is required to do the                        neighbours, then mark the node as ‘‘visited", leave one (any
above computations. The following result gives the answer, which
                                                                                       one) incoming edge alone and mark all other the incoming
confirms that Algorithm 1 achieves the fastest possible finite-time
                                                                                       edges as ‘‘removal", and then relay the token to all other
convergence for acyclic graphs.
                                                                                       neighbouring nodes without the ‘‘removal" mark, except the
                                                                                       remaining incoming edge;
Lemma 4. Under the linear complexity constraint (1) in Section 2, a
connected undirected graph G with diameter d needs a minimum of d                  (4) If it is already ‘‘visited" and it receives the token, mark the
iterations to achieve the fusion (20) or (21).                                         incoming edge as ‘‘removal" and do not relay the token
                                                                                       further.
Proof. Let nodes i and j be such that they are d hops away from each
other (such nodes exist by the definition of d). The information at
node i needs to propagate to node j in order for node j to correctly
fuse the CiT R− 1         T −1                                                      Algorithm 2 uses a max-consensus algorithm (Nejad, Attia, &
              i Ci (or Ci Ri yi (t)) term. By the linear complexity
constraint (1), this will take at least d iterations.                           Raisch, 2009), which is a distributed algorithm for finding the max-
                                                                                imum value in a connected graph. For a graph with diameter d and
Remark 5. Since our distributed estimate and its error covariance               n nodes (each node i with a variable ai ), the algorithm converges in
coincide with that of the central Kalman filter, our distributed                d iterations. For completeness, the algorithm is described below:
Kalman filter is stable due to the well-known fact that the central
Kalman filter is stable (Anderson & Moore, 1979).                                    • Each node i initializes ai (0) = ai and transmits it to its
                                                                                        neighbours.
                                                           Z. Wu et al. / Automatica 95 (2018) 63–72                                                         67


    • For iterations k = 1, 2, . . . , each node i updates ai (k) =               4.4. Distributed weighted least-squares estimation
      maxj∈Ni aj (k − 1) and transmits it to its neighbours.
                                                                                      Weighted least-squares estimation is no doubt one of the most
    As shown in Xie and Fu (2018), Algorithm 2 takes at most d + 1                popular estimation methods, applicable to a wide range of appli-
iterations to finish. Moreover, it has very low complexity for each               cations. Technically speaking, this is a special case of maximum
node because, during each iteration, each node relays the token                   likelihood estimation. The only difference is that the prior infor-
only to its neighbours which were not visited before.                             mation is not available for weighted least-squares estimation. This
                                                                                  is the same as taking Pt = ∞. That is, only the Termination step in
4. Properties                                                                     Algorithm 1 needs to be revised to
                                                                                    P̃t = Qi (d)−1 ;                                                       (22)
   In this section, we explore the properties, other than the finite
convergence property stated in Theorem 1. These properties in-                    x̃(t) = P̃t αi (d).                                                      (23)
clude robustness against transmission adversaries, asynchronous
implementability, and broadcast transmission mode. We will also                   4.5. Distributed estimation for multiple targets
discuss a special case of Algorithm 1 for distributed weighted least-
squares estimation and application of Algorithm 1 to multi-target                    The proposed distributed algorithm is readily applicable to
estimation.                                                                       track multiple targets. We modify the target model (1) to a set of
                                                                                  target models:
4.1. Robustness against transmission loss and delay
                                                                                  x(m) (t + 1) = A(m) x(m) (t) + w (m) (t), m = 1, 2, . . . , M .          (24)
   It turns out that Algorithm 1 is very resilient to transmission                                                                          (m)
                                                                                  For each target m, there may be a set of sensors S              monitoring it,
loss and delay due to the fact that each node uses the most
                                                                                  i.e., for each i ∈ S (m) ,
recently received information from its neighbours to update its
own information. A delayed arrival of information from neighbours                  (m)
                                                                                  yi (t) = Ci
                                                                                                (m) (m)
                                                                                                    x     (t) + v (m) (t).                                 (25)
will only delay the update and the convergence of the algorithm,
without affecting the maximum likelihood estimates. Likewise, if                  We can either run Algorithm 1 for M times in parallel, one for each
packet loss happens, it is sufficient that retransmission happens                 target model, or amalgamate the targets into a super-target and
and eventually the information arrives, which can be guaranteed                   then run Algorithm 1 on it. The two implementations may yield dif-
by most communications protocols, e.g., Transmission Control Pro-                 ferent communication complexities depending on whether trans-
tocol (TCP), via Acknowledgement (ACK). This will result in trans-                missions for different targets are synchronized or not, and whether
mission delay only and will not affect the maximum likelihood                     they can be packed together.
estimates. The only requirement to ensure the correctness of the
DKF result is that the delayed information needs to arrive within                 5. Application to distributed field estimation
one sampling period of the system.
                                                                                      In this section, we discuss how the proposed DKF algorithm
                                                                                  can be easily modified to solve the distributed field estimation
4.2. Asynchronous implementation
                                                                                  problem.
                                                                                      To illustrate the distributed field estimation problem, consider
    Although Algorithm 1 appears as a synchronous algorithm (i.e.,                monitoring the temperature distribution over a large geographical
all the nodes update together at each iteration time k), this is                  area using a network of temperature sensors. Each sensor mea-
purely for convenience. The algorithm can be implemented asyn-                    sures the temperature at its local point, with unavoidable measure-
chronously as follows: Each node waits till it receives new infor-                ment noise. To reduce the noise effect, all the measurements over
mation from its neighbours, then updates its own information and                  a region centred around a sensor are weight-averaged. To further
transmits it to its neighbours. In fact, each node can either update              improve the estimation accuracy, a dynamic temperature model
information as soon as it receives some update from neighbours,                   is incorporated to provide prior prediction of the temperature
or wait till multiple updates arrive or till a prescribed time interval           change, which can be developed using historical data (e.g., data
elapses. The only tradeoff is that more frequent updates require                  over past 24 h) or forecast information. Each sensor may have its
more transmissions and less frequent updates cause more delay.                    local temperature model, specialized to its local physical environ-
As discussed earlier, as long as the all the iterations finish within             ment. The prior prediction is then calibrated using the averaged
one sampling period, no degradation will occur to the DKF result.                 measurements. Although we have used temperature distribution
                                                                                  monitoring as an example, the same distributed field estimation
4.3. Point-to-point transmission vs. broadcast                                    problem occurs in vast applications, including air pollutant mon-
                                                                                  itoring, water quality monitoring, flow distribution monitoring,
    Algorithm 1 is written for point-to-point transmissions be-                   traffic congestion monitoring, data traffic monitoring, and so on.
tween neighbours. That is, each node i transmits to each                              We now formally describe our distributed field estimation
neighbouring node j different information, i.e., (Qi→j (k), αi→j (k)).            problem. Given a sensor network represented by a connected
Point-to-point transmission has the advantage of good privacy, but                undirected graph G = {V , E } as before, denote by Gi (k) a sub-graph
at the cost of more transmissions (one for each neighbour). An                    of G centred around node i such that all the nodes in it are at most
alternative is broadcast. Algorithm 1 can be easily implemented                   k hops away from node i. Each node i is associated with a state
in a broadcast mode. To do so, each node i still needs to compute                 variable xi (t) ∈ Rm of common interest to all nodes (such as the
(Qi→j (k), αi→j (k)) for each neighbouring node j, but simply broad-              temperature in the example above) at time t, and a linear dynamic
cast (Qi (k), αi (k)) to all of its neighbours. When node j receives              model:
(Qi (k), αi (k)), it can re-compute (Qi→j (k), αi→j (k)) by subtracting its
                                                                                  xi (t + 1) = Ai xi (t) + wi (t),                                         (26)
own (Qj→i (k − 1), αj→i (k − 1)). For applications like wireless sensor
networks where transmission power and bandwidth are scarce,                       where wi (t) is a zero-mean i.i.d. Gaussian noise with covariance
broadcasting is no doubt a better choice.                                         Wi ≥ 0, and xi (0) is an independent Gaussian variable with mean
68                                                         Z. Wu et al. / Automatica 95 (2018) 63–72


xi,0 and covariance Pi,0 > 0. Each node i also has a measurement
similar to (2), i.e.,

yi (t) = Ci xi (t) + vi (t).                                           (27)

Also given is an integer r ≤ d describing the size of the geographi-
cal region of interest, i.e., each node i is interested only in the sub-
network associated with Gi (r).
     In our distributed field estimation problem, we recognize the
fact that a field distribution tends to have high correlations among
neighbouring nodes. Instead of modelling the correlations explic-
itly (which is not always easy), we take the view that each node i
regards all the nodes in Gi (r) to have roughly the same state, i.e.,
xj (t) ≈ xi (t) for all j ∈ Gr . This means that, as far as node i is
concerned, there is a common system model for all the nodes in
Gi (r), which is (26), and the measurement yj (t) available at j ∈ Gi (r)
is
                                                                                                        Fig. 1. Sensor network for target tracking.
yj (t) ≈ Cj xi (t) + vj (t).                                           (28)

    Due to the above approximation, at each time step t, our dis-
tributed field estimation problem involves doing the following for                6.1. Single target tracking
each node i:
                                                                                     Consider a network of sensors in a two-dimensional region
     (1) Compute the regional maximum likelihood state estimate
                                                                                  to monitor a possible moving target (e.g., intruder, truck, mobile
         x̃i (t) = E {x(t)|x̂i (t), yj (t), ∀j ∈ Gi (r)}                          transmitter). The state of the target is x(t) = [pT (t) sT (t)]T with the
                                                                                  two-dimensional coordinate p(t) and its velocity s(t). The sampling
         and the associated estimation error covariance P̃i (t) using             period is assumed to be T , normalized to be 1 s. Its state-space
         the approximation (28).                                                  equation is a random walk model as follows:
     (2) Compute, using the above result and (26), the state estimate
         x̂i (t + 1) for xi (t + 1) and the associated estimation error           p(t + 1) = p(t) + s(t)T + wp (t),                                       (29)
         covariance Pi (t + 1).                                                   s(t + 1) = s(t) + ws (t).                                               (30)

    It is clear that when the region of measurements used for the                 Each active sensor i ∈ S measures p(t) with noise, i.e.,
first step is the entire sensor network and the state variables for
different nodes are the same, this problem is the same as the DKF                 yi (t) = p(t) + vi (t).                                                 (31)
problem we have studied so far. The challenge is, of course, how to
                                                                                  We assume that the noises w (t) = [wp (t) ws (t)] and vi (t) are    T
modify the proposed algorithm to solve this new problem.
                                                                                  independent, zero-mean, and their covariances are as follows:
    It turns out the required modification is very simple. Firstly, in-
                                                                                  W = diag{0.1, 0.05}, Ri = diag{0.08, 0.08}, P0 = diag{0.2, 0.2,
stead of running Algorithm 1 for d iterations, we stop at r iterations.
                                                                                  0.2, 0.2}.
Secondly, in the Termination step, Pt , P̃t , x̂(t) and x̃(t) need to be
                                                                                      We consider the square monitoring area in Fig. 1, with 7 × 7
replaced with their local versions by adding the subscript i. What
                                                                                  sensors uniformly distributed. It is assumed that each sensor can
these two changes do is that the maximum likelihood estimate
                                                                                  detect a target around it with the radius of 15. The original commu-
obtained at each node i becomes a regional estimate over Gi (r).
                                                                                  nication edges are depicted by the dotted lines., i.e., every node can
Finally, it is clear that the Prediction step can be easily replaced
                                                                                  communicate with any horizontal or vertical node next to it (or, put
with its local version. It is interesting to know that the modified
                                                                                  in another way, any node within the radius of 12). It is obvious that
algorithm has even lower complexities than the original algorithm
                                                                                  this graph is cyclic. Applying the distributed DFS algorithm results
due to the lower number of iterations required.
                                                                                  in a spanning tree with the communication edges depicted by the
    Due to the fact that only r iterations are needed, the graph G
                                                                                  solid lines in Fig. 1.
needs not be acyclic. This is because of the nice property of the
                                                                                      Fig. 2 shows the target tracking results by the distributed esti-
proposed algorithm that only nodes within r hops away from node
i are fused in x̃i (t). This means that it is sufficient to have G void of        mator and the central estimator. As expected by Theorem 1, there
loops of length r or less. Also note that central Kalman filtering is             is no difference between the two.
not directly applicable to the distributed field estimation because
each node has its own state estimation problem, i.e. there is no                  6.2. Multiple target tracking
central state to estimate.
                                                                                     We consider the same sensor network in Fig. 1 and add a second
6. Simulations                                                                    target with the following model:
                                                                                          0.9          0.9
                                                                                        [                  ]         [        ]
                                                                                                                1           0
   In this section, we provide several simulation examples to                      A=                       ,C=               .
                                                                                          −0.1         0.91     0           1
demonstrate the proposed DKF algorithm. Our attention will be on
the complexities of the algorithm and accuracy of the estimation                  The stochastic properties of the initial state and the noises of the
results. Three examples will be used. The first one is about tracking             measurements and the systems are same as in the first example.
of a single target in a surveillance network. The second one is a                 The noises for the two targets are independent.
simple generalization of the first one, about tracking of multiple                   The estimation results are shown in Fig. 3. Again, the results for
targets. The third one is about distributed field estimation for a                the distributed estimation and central estimation are the same, as
temperature field (e.g., in a bushfire monitoring network).                       expected.
                                                                  Z. Wu et al. / Automatica 95 (2018) 63–72                                                     69




                    Fig. 2. Single target tracking comparison.


                                                                                                                Fig. 4. Temperature distribution.




                                                                                         with ω = 2π/T0 with T0 = 24 × 60 min, and δi represents the local
                                                                                         temperature fluctuation. Fig. 4 shows the distribution of τi (t) with
                                                                                         t = 0 and τ0 (0) = 10 (i.e., Fig. 4 shows the distribution of δi + 10). It
                                                                                         is assumed in this example that δi is time-invariant. The sampling
                                                                                         period is chosen to be 1 min. The above can be modelled by
                                                                                                        [                ]
                                                                                                           0       1
                                                                                         xi (t + 1) =                   x (t)
                                                                                                          −1   2 cos(ω) i
                                                                                         with xi (0) = δi + 10. This gives τi (t) = [1 0]xi (t).
                                                                                            The measured temperature at node i is given by
                                                                                         yi (t) = τi (t) + vi (t) = [1 0]xi (t) + vi (t)
                                                                                         with vi (t) as a Gaussian noise with zero mean and variance of
                                                                                         0.09. The measured temperature distribution at t = 0 is shown in
                                                                                         Fig. 5.
                   Fig. 3. Multiple target tracking comparison.                             Choosing r = 4 as the size of geographical region of interest
                                                                                         and applying the proposed algorithm in Section 5, the resulting
                                                                                         estimated temperature distribution is shown in Fig. 6.
6.3. Distributed field estimation
                                                                                         6.4. Comparisons
   Consider a terrain (e.g., forest, coal mine, agriculture land,
                                                                                         6.4.1. Target tracking examples
chemical field) covered by a wireless sensor network to moni-
                                                                                            We first compare our simulation results for the two target
tor its temperature distribution. Each sensor is equipped with a
                                                                                         tracking examples with several classes of methods.
temperature sensor. A dynamic temperature model is available
at each sensor, which can be obtained from historical data and                           Sequential processing method: In a sequential processing
physical properties of the terrain. It is desirable to smooth out                        method (Zhao & Nehorai, 2007) (although it is also called dis-
the measurements over a region around each sensor so that mea-                           tributed method in this reference), the maximum likelihood es-
surement noises are reduced and local temperature fluctuations                           timation step (or Update step) (7)–(8) is done sequentially. More
are suppressed. Although it is possible to carry out the required                        specifically, nodes 1, 2, . . . , n are arranged in an ordered sequence
estimation work in a centralized way, this would involve a central                       and initialize x̃(t) = x̂(t). Then, node 1 updates x̃(t) using its local
processing unit and create communication bottlenecks around it.                          measurement y1 (t) and passes the updated x̃(t) to node 2. This goes
The example below shows how distributed estimation is done                               on until x̃(t) is finally updated by node n. Clearly, this sequential
using the proposed algorithm.                                                            processing doe not satisfy our distributed processing properties.
   A square field of one square kilometres has temperature sensors
                                                                                         Distributed updating, non-distributed averaging method: In the
uniformly distributed, one sensor for every 10 × 10 square metres.
At each node, the temperature τi (t) at each node i and time t is                        distributed estimation algorithm in Vadigepalli and Doyle (2003),
modelled by                                                                              each node takes the state estimate x̂i (k − 1|k − 1) using its
                                                                                         own prediction model to obtain x̂i (k|k − 1), then using its local
τi (t) = τ0 (t) + δi ,                                                                   measurement yi (k) to update its local estimate x̂i (k|yi (k)), then
                                                                                         communicate with all other nodes to obtain an average x̂i (k|k).
where τ0 (t) is the average field temperature at time t, which
                                                                                         Although the result is identical to global estimation, the second
experiences the following 24-hour cycle (a fictitious model):
                                                                                         step (communications among nodes and averaging) is not done
τ0 (t) = 10 + 5 sin(ωt)                                                                  in a distributed fashion. Thus, this does not satisfy our distributed
70                                                            Z. Wu et al. / Automatica 95 (2018) 63–72


                                                                                     but this compromises the optimality, and the number of iterations
                                                                                     cannot be determined a priori. In an example in Carli et al. (2008),
                                                                                     a randomly distributed network of 30 nodes needs 10 iterations,
                                                                                     but it is not clear how the required number of iterations grows as
                                                                                     the network size grows. In contrast, the proposed algorithm needs
                                                                                     only d iterations. Other references using the Laplacian matrix based
                                                                                     average consensus method include Das and Moura (2015), Kar and
                                                                                     Moura (2011), Xu et al. (2012) and Zhou et al. (2013), which suffer
                                                                                     from similar deficiencies.
                                                                                         In summary, all the methods above, estimation errors are iden-
                                                                                     tical or similar to the distributed algorithm given in this paper,
                                                                                     hence we do not show the corresponding simulation results for the
                                                                                     tracking examples. (The only case where estimation errors are not
                                                                                     identical is the Laplacian matrix-based average consensus method
                                                                                     with a finite number of iterations.) The main differences lie in
                                                                                     either lack of fully distributed processing properties (Methods 1–
                                                                                     3 above) or requiring higher computational and communicational
                                                                                     complexities (Methods 2 to 4 above).

                                                                                     6.4.2. Distributed field estimation example
                Fig. 5. Measured temperature distribution.                              We now do comparison for the distributed field estimation
                                                                                     example.
                                                                                     Cluster-based method: In Medeiros et al. (2008), a cluster-based
                                                                                     distributed Kalman filtering technique is proposed for target track-
                                                                                     ing. Nodes which can detect the target at a given time instant
                                                                                     forms a cluster. In this cluster, a node is chosen as a cluster head
                                                                                     which collects measurements from other nodes and updates the
                                                                                     state estimate collectively. This updated state estimate is passed
                                                                                     on to the cluster head in the next time instant. It is clear that
                                                                                     this cluster coincides with our set of sensing nodes S (t). However,
                                                                                     the cluster-based processing is still not fully distributed in our
                                                                                     sense. In particular, the cluster head is still a bottleneck for both
                                                                                     communication and computation, although not as severe as in a
                                                                                     fully centralized scheme.
                                                                                     Diffusion method: Instead of doing full average consensus, the
                                                                                     diffusion technique (e.g., Cattivelli & Sayed, 2010; Kanna et al.,
                                                                                     2015; Song, Yu et al., 2014) approximates a global average with an
                                                                                     average among the neighbouring nodes. This makes the algorithm
                                                                                     fully distributed, but the estimation performance becomes sub-
                                                                                     optimal.
                                                                                     Nearest neighbour interpolation-based method: In Martinez
                                                                                     (2010), an asynchronous Kalman filter-like algorithm is proposed
                Fig. 6. Estimated temperature distribution.                          for distributed field estimation by combining a nearest neighbour
                                                                                     interpolation method and Laplacian matrix-based average con-
                                                                                     sensus algorithm. More specifically, each sensor produces a local
                                                                                     estimate of a given field by fusing together its own measurement
processing properties. Vadigepalli and Doyle (2003) recognize this                   and information from neighbouring nodes via nearest neighbour
problem and suggest to do the averaging exercise at a reduced                        interpolation, then these local estimates are fused together using
sampling rate. This would compromise the optimality of the es-                       a Laplacian matrix-based average consensus algorithm. Again, the
timation.                                                                            required number of iterations for a given accuracy level cannot
Central processor assisted method: Some distributed estimation                       be determined a priori. In an example in Martinez (2010), for a
methods require a fusion centre which collects all the processed                     network of 75 nodes, 25 iterations are used, which appears high.
local information and fuse them together to produce a global esti-                   It is not clear how this scales up for a large network.
mate which is then broadcast back to each node; Chen et al. (2014a,                  Kriging interpolation method: In Cortés (2009), a distributed
b, 2015) and Song, Xu et al. (2014). Obviously these methods are                     field estimation algorithm is given based on the so-called Kriging
not fully distributed in our sense.                                                  interpolation technique. The algorithm employs the well-known
Laplacian matrix-based average consensus method: Many dis-                           Jacobi over-relaxation method and a dynamic average consensus
tributed Kalman filtering algorithms are available based on this                     method, both requiring an infinite number of iterations in theory. It
method, as mentioned in Introduction. Here we discuss Carli et                       is not clear how to choose the right number of iterations in practice,
al. (2008) as a representative algorithm. In Carli et al. (2008),                    especially for large networks.
each node i takes x̂(t) and computes its own update x̃i (t) using its                    In summary, the cluster-based method gives the same estima-
local measurement yi (t), then uses a Laplacian matrix based aver-                   tion result for our example, but it requires the selection of the
age consensus algorithm to produce the final update x̃(t), which                     cluster head and centralized processing in the cluster, hence it is
theoretically requires an infinite number of iterations to achieve                   not a fully distributed method in our sense. The Kriging interpo-
consensus. Carli et al. (2008) use a finite number of iterations,                    lation method gives a similar simulation result to the proposed
                                                                       Z. Wu et al. / Automatica 95 (2018) 63–72                                                                         71


                                                                                              Chen, B., Zhang, W.-A., & Yu, L. (2014a). Distributed finite-horizon fusion Kalman
                                                                                                   filtering for bandwidth and energy constrained wireless sensor networks. IEEE
                                                                                                   Transactions on Signal Processing, 62(4), 797–812.
                                                                                              Chen, B., Zhang, W.-A., & Yu, L. (2014b). Distributed fusion estimation with missing
                                                                                                   measurements, random transmission delays and packet dropouts. IEEE Trans-
                                                                                                   actions on Automatic Control, 59(7), 1961–1967.
                                                                                              Chen, B., Zhang, W.-A., Yu, L., Hu, G., & Song, H. (2015). Distributed fusion estimation
                                                                                                   with communication bandwidth constraints. IEEE Transactions on Automatic
                                                                                                   Control, 60(5), 1398–1403.
                                                                                              Cortés, J. (2009). Distributed Kriged Kalman filter for spatial estimation. IEEE Trans-
                                                                                                   actions on Automatic Control, 54(12), 2816–2827.
                                                                                              Das, S., & Moura, J. M. F. (2015). Distributed Kalman filtering with dynamic obser-
                                                                                                   vations consensus. IEEE Transactions on Signal Processing, 63(17), 4458–4473.
                                                                                              Durrant-Whyte, H. F., & Rao, B. S. (1991). Fully decentralised algorithm for multi-
                                                                                                   sensor Kalman filtering. IEE Proceedings D, 138(5), 413–420.
                                                                                              Goodrich, M. T., & Tamassia, R. (2001). Algorithm design: Foundations, analysis, and
                                                                                                   internet examples. Wiley.
                                                                                              Hashemipour, H. R., Sumit, R., & Laub, A. J. (1988). Decentralized structure for
                                                                                                   parallel Kalman filtering. IEEE Transactions on Automatic Control, 31(1), 88–94.
                                                                                              Hu, J., Xie, L., & Zhang, C. (2012). Diffusion Kalman filtering based on covariance
                                                                                                   intersection. IEEE Transactions on Signal Processing, 60(2), 891–902.
                                                                                              Kalman, R. E. (1960). A new approach to linear filtering and prediction problems.
        Fig. 7. Estimated temperature distribution using diffusion method.                         Transactions ASME-Journal of Basic Engineering, 82(D), 35–45.
                                                                                              Kanna, S., Dini, D. H., Xia, Y., Hui, S. Y., & Mandic, D. P. (2015). Distributed widely
                                                                                                   linear Kalman filtering for frequency estimation in power networks. IEEE Trans-
                                                                                                   actions on Signal and Information Processing over Networks, 1(1), 45–57.
method, when the iteration number is sufficiently large, but this                             Kar, S., & Moura, J. M. F. (2011). Gossip and distributed Kalman filtering: weak
is at the cost of more computation. The diffusion method and                                       consensus under weak detectability. IEEE Transactions on Signal Processing,
                                                                                                   59(4), 1766–1784.
nearest neighbour interpolation-based method give compatible
                                                                                              Khan, U. A., & Moura, J. M. F. (2008). Distributing the Kalman filter for large-scale
simulation results, which are shown in Fig. 7. We can see that they                                systems. IEEE Transactions on Signal Processing, 56(10), 4919–4935.
are inferior to the proposed method due to the fact that only the                             Li, D., Kar, S., Alsaadi, F. E., Dobaie, A. M., & Cui, S. (2015). Distributed Kalman filtering
information from the direct neighbours is used for each node in                                    with quantized sensing state. IEEE Transactions on Signal Processing, 63(19),
each sampling point.                                                                               5180–5193.
                                                                                              Li, D., Kar, S., Moura, J. M. F., Poor, H. V., & Cui, S. (2015). Distributed Kalman filtering
                                                                                                   over massive data sets: analysis through large deviations of random Riccati
7. Conclusions                                                                                     equations. IEEE Transactions on Information Theory, 61(3), 1351–1372.
                                                                                              Luo, B., & Wu, Y. C. (2013). Distributed clock parameters tracking in wire-
    In this paper, we have proposed a new distributed algorithm                                    less sensor network. IEEE Transactions on Wireless Communications, 12(12),
                                                                                                   6464–6475.
for Kalman filtering for sensor networks. The algorithm runs very
                                                                                              Mahmoud, M. S., & Khalid, H. M. (2013). Distributed Kalman filtering: a bibliographic
efficiently on acyclic network graphs, with very low complexities                                  review. IET Control Theory & Applications, 7(4), 483–501.
for each sensor node. It is also applicable to cyclic network graphs                          Marelli, D. E., & Fu, M. (2015). Distributed weighted least-squares estimation with
by running a distributed loop removal algorithm on the graph first,                                fast convergence for large-scale systems. Automatica, 51, 27–39.
which is also a very efficient with very low complexities. The pro-                           Martinez, S. (2010). Distributed interpolation schemes for field estimation by mo-
                                                                                                   bile sensor networks. IEEE Transactions on Control Systems Technology, 18(2),
posed algorithm provides the same estimation quality as a central
                                                                                                   491–500.
Kalman filter and enjoys many extraordinary features including                                Medeiros, H., Park, J., & Kak, A. C. (2008). Distributed object tracking using a cluster-
robustness against transmission adversaries, asynchronous imple-                                   based Kalman filter in wireless camera networks. IEEE Journal of Selected Topics
mentability, and broadcast mode operation. The technical core of                                   in Signal Processing, 2(4), 448–463.
the algorithm is a distributed algorithm for maximum likelihood                               Nejad, B. M., Attia, S. A., & Raisch, J. (2009). Max-consensus in a max-plus algebraic
estimation, including weighted least-squares estimation as a spe-                                  setting: the case of fixed communication topologies. In XXII International sym-
                                                                                                   posium on information, communication and automation technologies.
cial case. The algorithm is most suitable for detection, estimation
                                                                                              Regazzoni, C. S. (1994). Distributed extended Kalman filtering network for estima-
and tracking of dynamic targets. With simple modifications, the                                    tion and tracking of multiple objects. Electronics Letters, 30(15), 1202–1203.
algorithm is also suitable for an important application domain                                Riberio, A., & Giannakis, G. B. (2006). Bandwidth-constrained distributed estimation
called distributed field estimation requiring low-complexity dis-                                  for wireless sensor networks-Part I: Gaussian case. IEEE Transactions on Signal
tributed solutions. We expect that this algorithm will find wide                                   Processing, 54(3), 1131–1143.
                                                                                              Riberio, A., Giannakis, G. B., & Roumeliotis, S. I. (2006). SOI-KF: Distributed Kalman
applications in large-scale sensor networks. Our simulation ex-
                                                                                                   filtering with low-cost communications using the sign of innovations. IEEE
amples have demonstrated this potential. Future research topics                                    Transactions on Signal Processing, 54(12), 4782–4795.
include handling correlated measurement noises among sensors,                                 Roshany-Yamchi, S., Cychowski, M., Negenborn, R. R., De Schutter, B., Delaney, K.,
transmission failures between sensors and quantization errors in                                   & Connell, J. (2013). Kalman filter-based distributed predictive control of large-
the transmissions.                                                                                 scale multi-rate systems: application to power networks. IEEE Transactions on
                                                                                                   Control Systems Technology, 21(1), 27–39.
                                                                                              Shen, X., Song, E., Zhu, Y., & Luo, Y. (2009). Globally optimal distributed Kalman
References                                                                                         fusion with local out-of-sequence-measurement updates. IEEE Transactions on
                                                                                                   Automatic Control, 54(8), 1928–1934.
Anderson, B. D. O., & Moore, J. B. (1979). Optimal filtering. NJ: Prentice-Hall Engle-        Skiena, S. (2008). The algorithm design manual. Springer.
    wood Cliffs.                                                                              Song, E., Xu, J., & Zhu, Y. (2014). Optimal distributed Kalman filtering fusion with
Carli, R., Chiuso, A., Schenato, L., & Zampieri, S. (2008). Distributed Kalman filtering           singular covariances of filtering errors and measurement noises. IEEE Transac-
    based on consensus strategies. IEEE Journal on Selected Areas in Communications,               tions on Automatic Control, 59(5), 1271–1282.
    26(4), 622–633.                                                                           Song, H., Yu, L., & Zhang, W.-A. (2014). Distributed consensus-based Kalman filtering
Cattivelli, F. S., & Sayed, A. H. (2010). Diffusion strategies for distributed Kalman fil-         in sensor networks with quantised communications and random sensor fail-
    tering and smoothing. IEEE Transactions on Automatic Control, 55(9),                           ures. IET Signal Processing, 8(2), 107–118.
    2069–2084.
72                                                                    Z. Wu et al. / Automatica 95 (2018) 63–72


Sun, Y., Fu, M., Wang, B., & Zhang, H. (2015). A distributed MAP approach to                                           Minyue Fu received his Bachelor’s Degree in Electrical
     dynamic state estimation with applications in power networks. In European                                         Engineering from the University of Science and Technol-
     control conference, July.                                                                                         ogy of China, Hefei, China, in 1982, and M.S. and Ph.D.
Tai, X., Lin, Z., Fu, M., & Sun, Y. (2013). A new distributed state estimation technique                               degrees in Electrical Engineering from the University of
     for power networks. In American control conference, Washington D.C., June.                                        Wisconsin-Madison in 1983 and 1987, respectively. From
                                                                                                                       1983 to 1987, he held a teaching assistantship and a
Talarico, S., Schmid, N. A., Alkhweldi, M., & Valenti, M. C. (2014). Distributed
                                                                                                                       research assistantship at the University of Wisconsin-
     estimation of a parametric field: algorithms and performance analysis. IEEE
                                                                                                                       Madison. He worked as a Computer Engineering Con-
     Transactions on Signal Processing, 62(5), 1041–1053.                                                              sultant at Nicolet Instruments, Inc., Madison, Wisconsin,
Vadigepalli, R., & Doyle III, F. J. (2003). A distributed state estimation and control                                 during 1987. From 1987 to 1989, he served as an Assistant
     algorithm for plantwide processes. IEEE Transactions on Control Systems Tech-                                     Professor in the Department of Electrical and Computer
     nology, 11(1), 119–127.                                                                 Engineering, Wayne State University, Detroit, Michigan. He joined the Department
Wang, S., Fang, H., & Liu, X. (2015). Distributed state estimation for stochastic non-       of Electrical and Computer Engineering, the University of Newcastle, Australia,
     linear systems with random delays and packet dropouts. IET Control Theory &             in 1989. Currently, he is a Chair Professor in Electrical Engineering and Head of
     Applications, 9(18), 2657–2665.                                                         School of Electrical Engineering and Computer Science. In addition, he was a Visiting
Wang, Y., Ishwar, P., & Saligrama, V. (2008). One-bit distributed sensing and coding         Associate Professor at University of Iowa in 1995–1996, and a Senior Fellow/Visiting
                                                                                             Professor at Nanyang Technological University, Singapore, 2002. He has held a Qian-
     for field estimation in sensor networks. IEEE Transactions on Signal Processing,
                                                                                             ren Professorship at Zhejiang University and Guangdong University of Technology,
     56(9), 4433–4445.
                                                                                             China. He is a Fellow of IEEE. His main research interests include control systems,
Xiao, L., & Boyd, S. (2004). Fast linear iterations for distributed averaging. Systems &     signal processing and communications. He has been an Associate Editor for the IEEE
     Control Letters, 53, 65–78.                                                             Transactions on Automatic Control, Automatica and Journal of Optimization and
Xie, K., Cai, Q., Zhang, Z., & Fu, M. (2018). A fast convergent distributed algorithm for    Engineering.
     weighted average consensus. IEEE Transactions on Automatic Control submitted
     for publication.
Xu, J., Song, E., Luo, Y., & Zhu, Y. (2012). Optimal distributed Kalman filtering                                      Yong Xu was born in Zhejiang Province, China, in 1983.
     fusion algorithm without invertibility of estimation error and sensor noise                                       He received the B.S. degree in information engineering
                                                                                                                       from Nanchang Hangkong University, Nanchang, China, in
     covariances. IEEE Signal Processing Letters, 19(1), 55–58.
                                                                                                                       2007, the M.S. degree in control science and engineering
Zhao, T., & Nehorai, A. (2007). Information-driven distributed maximum likelihood
                                                                                                                       from Hangzhou Dianzi University, Hangzhou, China, in
     estimation based on Gauss-Newton method in wireless sensor networks. IEEE                                         2010, and the Ph.D. degree in control science and en-
     Transactions on Signal Processing, 55(9), 4669–4682.                                                              gineering from Zhejiang University, Hangzhou, China, in
Zhou, T. (2013). Coordinated one-step optimal distributed state prediction for a                                       2014. He was a visiting internship student with the depart-
     networked dynamical system. IEEE Transactions on Automatic Control, 58(11),                                       ment of Electronic and Computer Engineering, Hong Kong
     2756–2771.                                                                                                        University of Science and Technology, from June 2013
Zhou, Z., Fang, H., & Hong, Y. (2013). Distributed estimation for moving target                                        to November 2013. He was honored Pearl River Young
     based on state-consensus strategy. IEEE Transactions on Automatic Control, 58(8),       Scholars Program of Guangdong Province in 2017. Now he is an associate professor
     2096–2101.                                                                              with School of Automation, at Guangdong University of Technology, Guangzhou,
                                                                                             China.
                                                                                                 His research interests include networked control systems, state estimation, and
                                                                                             positive systems.
                        Zongze Wu received his B.S. degree in material forming
                        and control, M.S. degree in control science and engineer-
                        ing, and Ph.D. degree in pattern reorganization and intel-                                  Renquan Lu received his Ph.D. degree in Control Science
                        ligence system, all from Xi’an Jiaotong University, Xi’an,                                  and Engineering from Zhejiang University, Hangzhou,
                        China, in 1999, 2002, and 2005, respectively. He is cur-                                    China, in 2004. He was supported by the National Sci-
                        rently a professor of the School of Automation, Guangdong                                   ence Fund for Distinguished Young Scientists of China
                        University of Technology, Guangzhou, China. His research                                    in 2014, honored as the Distinguished Professor of Pearl
                        interests include Automation Control, Signal Processing,                                    River Scholars Program of Guangdong Province, the Dis-
                        Big Data and Internet of Things. He has served as the                                       tinguished Professor of Yangtze River Scholars Program
                        Under-Secretary-General for Internet of Things and In-                                      by the Ministry of Education of China in 2015 and 2017,
                        formation Technology Innovation Alliance in Guangdong                                       respectively. Currently, he is a professor of the School
Province, China. Dr. Wu was the recipient of the Microsoft Fellowship Award of the                                  of Automation at Guangdong University of Technology,
MSRA in 2003. He won the Technological Award first prize of Guangdong Province                                      Guangzhou, China. His research interests include complex
three times in 2008, 2013 and 2014, respectively. He got second prize of Ministry            systems, networked control systems, and nonlinear systems.
of Education technological innovation twice, in 2012 and 2013.
