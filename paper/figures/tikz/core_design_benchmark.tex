\documentclass[tikz,border=6pt]{standalone}
\input{figure_style.tex}

\begin{document}
\begin{tikzpicture}
  \begin{groupplot}[
    group style={group size=2 by 1,horizontal sep=15mm},
    paper axis,
    width=76mm,
    height=54mm,
  ]
    \nextgroupplot[
      title={(a) State smoother},
      xmin=-0.25,xmax=2.25,
      ymin=0.5,ymax=1.04,
      xtick={0,1,2},
      xticklabels={0-hop,1-hop,Full port},
      ylabel={Certified radius / 0-hop radius},
      ymajorgrids,
    ]
      \addplot[
        only marks,
        mark=o,
        mark size=1.45pt,
        draw=midgray,
        fill=white,
        opacity=0.62,
      ] table[col sep=comma,x=x,y=normalized_radius]
        {data/core_round_tradeoff_raw.csv};
      \addplot[
        oiBlue,
        paper mark,
        mark=*,
        error bars/.cd,
        y dir=both,
        y explicit,
      ] table[
        col sep=comma,
        x=x,
        y=median,
        y error plus expr={\thisrow{q75}-\thisrow{median}},
        y error minus expr={\thisrow{median}-\thisrow{q25}}
      ] {data/core_round_tradeoff_summary.csv};

    \nextgroupplot[
      title={(b) Design grid},
      xmin=0,xmax=65,
      ymin=0,ymax=1.02,
      xlabel={Exact-revalidated risk overhead (\%)},
      ylabel={Empirical CDF},
      xmajorgrids,
      ymajorgrids,
      legend style={at={(0.98,0.05)},anchor=south east},
    ]
      \addplot[oiBlue,solid,line width=1.15pt]
        table[col sep=comma,x=overhead_percent,y=fraction]
        {data/core_sampling_overhead_cdf.csv};
      \addlegendentry{Sampling, $N=32$}
      \addplot[oiOrange!90!black,dashed,line width=1.15pt]
        table[col sep=comma,x=overhead_percent,y=fraction]
        {data/core_psd_overhead_cdf.csv};
      \addlegendentry{$0\preceq X\preceq I$}
  \end{groupplot}
\end{tikzpicture}
\end{document}
